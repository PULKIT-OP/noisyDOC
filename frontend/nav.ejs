<nav class="topbar">
  <a class="brand" href="/">noisyDOC</a>
  <% if (currentUser) { %>
    <div class="topbar-center">
      <a href="/my-pdfs">My PDFs</a>
      <a href="/chat">New PDF</a>
    </div>
    <% } %>
      <div class="topbar-actions">
        <% if (currentUser) { %>
          <div class="user-pill">
            <span>
              <%= currentUser.email %>
            </span>
            <span>▾</span>
          </div>
          <a class="text-link" href="/logout">Logout</a>
          <% } else { %>
            <a class="text-link" href="/signup">Signup</a>
            <a class="text-link" href="/login">Login</a>
            <% } %>
      </div>
</nav>